<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= titulo %></title>
  <link rel="icon" href="/img/favicon.png" type="image/png">
  
  <link rel="stylesheet" href="/css/intranet.css">

  <style>
      /* 1. CONFIGURACIÓN DEL FONDO DE PANTALLA */
      html, body {
        height: 100%;
        margin: 0;
        padding: 0;
        width: 100%;
      }

      body {
        /* Asegúrate que la imagen exista en public/img/fondo-home.png */
        background: url('/img/fondo-home.png') no-repeat center center fixed !important;
        background-size: cover !important;
        display: flex;
        justify-content: center;
        align-items: center;
        font-family: Arial, sans-serif; /* Asegurar fuente legible */
      }

  </style>
</head>

<body>

  <div class="card-login">
    
    <div style="text-align: center; margin-bottom: 20px;">
      <img src="/img/favicon2.png" alt="Transworld" style="max-width: 180px; height: auto;"> 
    </div>

    <h2>Acceso a la intranet</h2>
    
    <% if (locals.error) { %>
      <div class="mensaje-error" style="text-align: center;"><%= error %></div>
    <% } %>
    
    <% if (locals.info) { %>
      <div class="mensaje-info" style="text-align: center;"><%= info %></div>
    <% } %>

    <form action="/login" method="POST">
      <label for="username">Email</label>
      <input type="text" id="username" name="username" required autofocus placeholder="nombre@transworld.cl">

      <label for="password">Contraseña</label>
      <input type="password" id="password" name="password" required placeholder="••••••••">

      <button type="submit">Ingresar</button>
    </form>

    <div class="login-links">
      <a href="/register">Crear cuenta</a> | 
      <a href="/forgot-password">¿Olvidaste tu contraseña?</a>
    </div>
  </div>

</body>
</html>